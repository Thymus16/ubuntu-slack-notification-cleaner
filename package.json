{
  "name": "ubuntu-slack-notification-cleaner",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check extension.js && node --check matching.js && node --test"
  }
}
